# Handoff — part3 → part4 · 2026-08-04 · PI-05

## Ticket and scope

PI-05 — `/flow` parity, route explanation, and plain-language waiting status.
Only PI-05 was worked. Claim path: local-file tracker `Debugger Ready` →
`Debugging` → `Grading Ready`. PI-06 and all Phase 2 work were not started.

Tracker mode is local `tickets.md`; this repository has no GitHub Project and no
git remote. No push was attempted or claimed.

## Actual profiles

- Maker: Pi harness · `openai-codex/gpt-5.6-terra` · xhigh, as recorded in the
  part2 PI-05 handoff.
- Debugger: Codex part3 session. The repository pin is GPT-5.6 Luna/max; this
  runtime exposes the model as GPT-5/Codex rather than a versioned Luna
  identifier. No helper was spawned and no model was silently substituted.

## Four-net audit

- Baseline failing tests: none. The locked gate was green before the audit: 12
  targeted tests and clean `tsc`.
- Baseline static errors: none.
- Weak or uncovered areas: unknown/missing stage data, duplicate ordering,
  waiting-state transitions and fallback wording, terminal/control injection,
  long and Unicode route reasons, ANSI visible width, narrow/non-finite widths,
  malformed/stale readings, failed data getters, input handling, and panel
  performance.
- Invariant review: the original panel could display unknown stages as stages,
  fabricate malformed numeric output (`NaN`/`Infinity`), leak raw terminal
  controls/newlines and secret-shaped text, overflow below its hard-coded
  8-column frame, throw on failed getters, and treat old agent readings as
  current because no agent-update timestamp reached the panel.

## Red-team fixes

- Stable-sort only known `part1`–`part4` stages; missing/unknown stages render as
  helpers, retain duplicate order, and never receive a stage percentage.
- Sanitize and redact route reasons, waiting events, task previews, agent
  titles/IDs, model labels, capability names, paths, and statuses before theme
  styling; collapse them to one physical line and scrub URLs and secret-shaped
  values.
- Use the same explicit numerator/denominator reading discipline for context
  percentages; malformed counters, timestamps, clocks, and readings degrade to
  safe indeterminate output. Agent-update timestamps now drive stale detection,
  with stale rows marked `~`.
- Catch failed state, agent, context, capability, and session getters; preserve
  the no-agent fallback and avoid fabricated values.
- Normalize zero, negative, narrow, and non-finite widths; keep every frame line
  within the requested visible width, including ANSI-themed and Unicode content.
- Keep tab/arrow/escape behavior bounded and cover a 1,000-render performance
  budget.
- Rendering remains free of filesystem, network, and subprocess calls; the
  source-scope purity assertion remains in the exact ticket tests.

## Verification evidence

Exact ticket gate, exit 0:

```text
node --test --experimental-strip-types extensions/workflow/flow-panel.test.ts extensions/workflow/policy.test.ts && npm run check
```

Result: 21 targeted tests passed; `tsc --noEmit` passed.

Additional gates, all exit 0:

- `npm test` — 164 Node tests and 22 Vitest tests.
- `npm run format:check`.
- `git diff --check`.

Repair commit: `de9aa4bc00ecd157ab9be0d913aa3a45ef93f58d` (parent
`e191ff25887528bf6049ca92e2b66ba7000abebd`).

## Honest follow-up and next review

No PI-05 follow-up remains. The full production workflow → subagents event-bus
round trip is still outside this ticket's harness, as recorded by prior stages.
Part4 should grade this repair commit blind and must not close PI-05 from this
handoff.
