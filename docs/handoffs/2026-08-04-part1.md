# Handoff — part1 → part2 · 2026-08-04

**Effort.** Persistent workflow/subagent progress UI in the Pi footer, `/flow` UX, then local-only token savings plus Windows portability.

**Artifacts.**
- Spec + invariants: `docs/2026-08-04-flow-ui-and-token-savings.md`
- Tracker: `tickets.md` (PI-01 … PI-08) — local-file tracker, this repo has no GitHub Project
- Baseline gate proven green before planning: `npm run check` (tsc clean) and `npm test` (22 passed)

**Locked decisions.**
1. Adaptive footer — 3 base lines + up to 4 stage rows, shown only while that stage agent exists (7 lines max).
2. Percentages only from a real denominator: context tokens/window, question `k/N`, stage `k/4`, and tickets done/total. Everything else renders as glyph + elapsed + turns.
3. Local-only token savings — no proxy, no new network hop — and the whole workflow must also run on the user's Windows laptop.

**Invariants** (full text in the spec): INV-1 no-false-progress · INV-2 no-secret · INV-3 pure ≤2 ms render · INV-4 ≤7 lines / ≤4 stage rows · INV-5 stale is visibly stale at 30 s · INV-6 degrade never fabricate never crash · INV-7 portability · INV-8 no new trust edge.

**Audit result** (why nothing external is being adopted):
- Ponytail 4.8.4 is *already installed* as a Pi package via `~/.pi/agent/settings.json` but is invisible to this repo — no mention in `README.md`, `SYSTEM.md`, or `settings.example.json`. PI-06 makes it explicit and pinned.
- Caveman exists only as a Claude Code skill; Pi has no equivalent. PI-06 ports the rules, including its safety exceptions.
- Anthropic traffic *already* traverses a third-party proxy (`agentrouter.org`, per runtime `models.json`). Adding a second compressing proxy would widen that trust edge and break prompt caching, so OmniRoute/RTK is out of scope under INV-8.
- **Headroom proxy: no artifact found on this machine.** I could not assess it from evidence and did not guess. It stays out of scope.
- CodeGraph / external structural retrieval: excluded by decision 3; reopen in a future part1 run if wanted.

**Slice order.** Phase 1: PI-01 → PI-02 → PI-03 → PI-04 → PI-05. Phase 2 (must not start before PI-04 lands): PI-06 → PI-07 → PI-08.

**Next agent starts at PI-01** — the only ticket in `Agent Ready`. Its gate:
`node --test --experimental-strip-types extensions/shared/workflow-state.test.ts extensions/workflow/policy.test.ts && npm run check`

**Blocker to surface to the user (not resolvable by an agent).** `git remote -v` is empty — this repo has no remote, so `push-handoff` cannot produce remote proof. Planning artifacts are committed locally only. Recovery, when the user chooses: `gh repo create <name> --private --source=. --push` (the `gh` CLI is authenticated as `VrajGupta` with `repo`, `project`, `workflow` scopes). Runtime secrets stay out: `.gitignore` already excludes `auth.json`, `models.json`, `settings.json`, `trust.json`, `.env*`.
